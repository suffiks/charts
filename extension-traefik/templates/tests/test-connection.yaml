apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "extension-traefik.fullname" . }}-test-connection"
  labels:
    {{- include "extension-traefik.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "extension-traefik.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
